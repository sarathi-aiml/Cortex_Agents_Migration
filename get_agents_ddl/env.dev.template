# =============================================================================
# SNOWFLAKE CORTEX AGENT DDL GENERATOR CONFIGURATION TEMPLATE
# =============================================================================
# Copy this file to env.dev and fill in your actual values
# DO NOT commit env.dev to version control!
#
# Command to create your env.dev file:
#   cp env.dev.template env.dev
#   # Then edit env.dev with your actual credentials
# =============================================================================

# =============================================================================
# SOURCE ACCOUNT CONFIGURATION
# =============================================================================
# Your Snowflake account URL (format: https://<ACCOUNT_IDENTIFIER>.snowflakecomputing.com)
SOURCE_ACCOUNT_URL=https://<YOUR_ACCOUNT_IDENTIFIER>.snowflakecomputing.com

# Personal Access Token (PAT) for authentication
# To create a PAT:
#   1. Log into Snowflake web UI
#   2. Navigate to Admin → Users & Security → Personal Access Tokens
#   3. Click Generate Token
#   4. Copy the token and paste it here
SOURCE_PAT=<YOUR_PERSONAL_ACCESS_TOKEN>

# Your Snowflake username
SOURCE_USER=<YOUR_USERNAME>

# Warehouse to use for connections
SOURCE_WAREHOUSE=<YOUR_WAREHOUSE_NAME>

# Default database and schema where agents are located
SOURCE_DATABASE=<YOUR_DATABASE_NAME>
SOURCE_SCHEMA=<YOUR_SCHEMA_NAME>

# =============================================================================
# OPTIONAL: Default Agent (for reference/documentation)
# =============================================================================
# Default agent name (optional, for documentation purposes)
SOURCE_DEFAULT_AGENT=<OPTIONAL_DEFAULT_AGENT_NAME>

